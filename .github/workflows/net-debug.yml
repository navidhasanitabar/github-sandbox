name: Net Debug

on:
  workflow_dispatch:
    inputs:
      url:
        description: "Target URL"
        required: true
        type: string

      forced_ip:
        description: "Optional forced IP for /etc/hosts"
        required: false
        type: string

jobs:
  debug:
    runs-on: ubuntu-latest

    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install tools
        run: |
          sudo apt-get update

          sudo apt-get install -y \
            dnsutils \
            curl \
            wget \
            aria2 \
            openssl \
            traceroute \
            mtr \
            netcat-openbsd \
            iproute2 \
            iputils-tracepath \
            jq \
            whois

      - name: Run diagnostics
        env:
          URL: ${{ github.event.inputs.url }}
          FORCED_IP: ${{ github.event.inputs.forced_ip }}
        run: |
          chmod +x scripts/net-debug.sh
          ./scripts/net-debug.sh "$URL"

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: net-debug-logs
          path: logs/